/**
 * prompt-panel feature - 节点提示词面板
 *
 * 简化版(Phase C.4):textarea + 生成/停止按钮 + 模式标签。
 * 依赖项 ModelPicker / SettingsPopover / PromptLibrary / ResourceMention
 * 留待 Phase E 补齐(届时扩展 Props 与子组件装配)。
 */

export { PromptPanel } from './components/prompt-panel.js';
export type { PromptPanelProps, GenerationMode } from './components/prompt-panel.js';
